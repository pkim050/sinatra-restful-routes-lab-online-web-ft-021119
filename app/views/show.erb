<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Homepage</title>
  </head>
  <body>
    <h1>All Recipes</h1>
    <% @recipes.each do |element| %>
      <div name="recipes">
        <h2><%= element.name %></h2><br>
        <p>Ingredients you'll need: <%= element.ingredients %></p><br>
        <p>Time Duration: <%= element.cook_time %></p><br>
        <a href="/recipes/<%= element.id %>">Link to Recipe</a>
      </div>
    <% end %>
  </body>
</html>
